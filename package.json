{
  "private": true,
  "scripts": {
    "build:icons": "esbuild morph-icons.source.js --bundle --format=iife --minify --outfile=recursos/librerias/morphicons.bundle.js"
  },
  "dependencies": {
    "morphicons": "1.7.1"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
